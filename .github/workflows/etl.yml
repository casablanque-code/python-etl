<<<<<<< HEAD
name: Build ETL (XLSX/CSV/XML ➜ Supabase CSV)

on:
  push:
  workflow_dispatch:

=======
name: Build ETL v3 (strict Supabase CSV)
on: [push, workflow_dispatch]
>>>>>>> eb76ea2 (ETL_1.2)
jobs:
  etl:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
<<<<<<< HEAD

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run ETL
        run: |
          python -m scripts.etl

      - name: Pack outputs
        run: |
          cd output
          zip -r ../etl_outputs.zip . || true

      - name: Upload artifact
        uses: actions/upload-artifact@v4
=======
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: |
          python -m pip install -r requirements.txt
          python -m scripts.etl
      - run: |
          cd output && zip -r ../etl_outputs.zip . || true
      - uses: actions/upload-artifact@v4
>>>>>>> eb76ea2 (ETL_1.2)
        with:
          name: etl_outputs
          path: etl_outputs.zip
